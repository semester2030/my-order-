# 🏗️ هيكل منصة الإدارة الشاملة (Admin Panel)

## رأي تنفيذي

- **منصة واحدة** للإدارة (وليس لوحة للمطاعم وأخرى للسائقين).
- **أدوار متعددة** داخل نفس المنصة: واجهات وصلاحيات حسب الدور.
- **موافقات + تشغيل + مراقبة + تحكم + منع تلاعب + سجلات** كلها تحت سقف واحد.

---

## 1. الهيكل العام للمنصة

```
admin_panel/                    ← تطبيق ويب واحد (منفصل عن customer / driver / vendor)
├── auth/                       ← تسجيل دخول الأدمن
├── dashboard/                  ← لوحة رئيسية حسب الدور
├── vendors/                    ← إدارة المطاعم + الموافقات
├── drivers/                    ← إدارة السائقين + الموافقات
├── orders/                     ← الطلبات (عرض، تتبع، تدخل)
├── payments/                   ← المدفوعات والمصالحة
├── disputes/                   ← الشكاوى وحلها
├── monitoring/                 ← المراقبة اليومية (حرجة)
├── risk_engine/                ← قواعد منع التلاعب
├── settings/                  ← إعدادات المنصة والأدوار
└── audit_logs/                 ← سجل كل فعل (قانوني + تشغيلي)
```

---

## 2. الأدوار (Roles) والصلاحيات

| الدور | الوحدات الظاهرة | الصلاحيات الرئيسية |
|-------|-----------------|---------------------|
| **Super Admin** | الكل | كل شيء: موافقات، إعدادات، أدوار، مراقبة، مخاطر، سجلات |
| **Ops Admin** | Vendors, Drivers, Orders, Monitoring | موافقة مطاعم/سائقين، عرض وتعديل طلبات، مراقبة حية |
| **Finance** | Payments | مدفوعات، مصالحة، تقارير مالية فقط |
| **Support** | Orders, Disputes, Monitoring | تتبع طلبات، حل شكاوى، مراقبة دون موافقات |
| **Quality** | Vendors, Drivers (قراءة + تقييم) | تقييم مطاعم وسائقين، لا موافقة ولا دفعات |

**نفس المنصة — واجهات مختلفة حسب الدور.**

---

## 3. الوحدات والصفحات (تفصيل)

### 3.1 Auth
- `auth/login` — تسجيل دخول (بريد/هاتف + كلمة مرور أو SSO لاحقاً).
- إعادة توجيه حسب الدور بعد Login.

### 3.2 Dashboard
- `dashboard` — لوحة رئيسية:
  - **Super / Ops:** طلبات اليوم، طلبات حية، مطاعم معلقة موافقة، سائقون معلقون، تنبيهات.
  - **Finance:** إجمالي مدفوعات، مصالحات معلقة.
  - **Support:** شكاوى مفتوحة، طلبات متأخرة.
  - **Quality:** تقييمات منخفضة، تقارير جودة.

### 3.3 Vendors (المطاعم)
- `vendors` — قائمة المطاعم (فلاتر: حالة، تاريخ).
- `vendors/applications` — **طلبات التسجيل (الموافقات):**
  - بيانات المطعم، المستندات، الفيديوهات، Signature items.
  - أزرار: **Approve** | **Reject** | **Needs changes** | **Suspend** (لاحقاً).
- `vendors/[id]` — تفاصيل مطعم (بيانات، قائمة، طلبات، تقييمات).
- `vendors/[id]/edit` — تعديل (حسب الصلاحية).

### 3.4 Drivers (السائقون)
- `drivers` — قائمة السائقين (فلاتر: حالة، تاريخ).
- `drivers/applications` — **طلبات التسجيل (الموافقات):**
  - الهوية، الرخصة، السيارة، التحقق من الخلفية.
  - أزرار: **Approve** | **Reject** | **Suspend** | **Probation**.
- `drivers/[id]` — تفاصيل سائق (طلبات، أرباح، تقييمات).
- `drivers/[id]/edit` — تعديل (حسب الصلاحية).

### 3.5 Orders
- `orders` — قائمة الطلبات (فلاتر: حالة، تاريخ، مطعم، سائق).
- `orders/live` — طلبات حية (خريطة/جدول).
- `orders/[id]` — تفاصيل طلب (إمكانية تدخل: إلغاء، إعادة تعيين سائق، ملاحظات).

### 3.6 Payments
- `payments` — قائمة المعاملات المالية.
- `payments/reconciliation` — مصالحة (مطاعم، سائقون، عمولة).
- تقارير وإيرادات (للـ Finance).

### 3.7 Disputes
- `disputes` — قائمة الشكاوى (عميل، مطعم، سائق).
- `disputes/[id]` — تفاصيل وحل (تعليق، قرار، تعويض إن وجد).

### 3.8 Monitoring (المراقبة اليومية — حرجة)
- `monitoring` — لوحة مراقبة:
  - **طلبات حية** (حالة، تأخير).
  - **تأخير سائق** (متوسط، حالات متطرفة).
  - **مطعم يرفض كثيراً** (نسبة رفض).
  - **سائق يلغي كثيراً** (نسبة إلغاء).
  - **Drop rate**، **Average prep time**، وغيرها حسب المقاييس التي تضعها.

### 3.9 Risk Engine (منع التلاعب)
- `risk_engine` — قواعد ومُعلّمات:
  - سائق يغيّر موقعه فجأة → Flag / مراجعة.
  - مطعم يرفع سعر بعد الطلب → Flag / تعليق.
  - عميل يلغي باستمرار → Flag / حد.
  - Vendor يرفض > X% → تنبيه / تعليق.
- نتيجة القاعدة: **Flag** | **Suspend مؤقت** | **Review يدوي** (حسب السياسة).

### 3.10 Settings
- `settings` — إعدادات عامة المنصة (ساعات، مناطق، رسوم).
- `settings/roles` — إدارة الأدوار والصلاحيات (Super Admin فقط).

### 3.11 Audit Logs
- `audit_logs` — سجل لكل فعل:
  - من وافق / رفض / عدّل؟
  - متى؟
  - ماذا تغير؟ (قبل/بعد إن أمكن).
  - لماذا رُفض؟ (حقل سبب مطلوب عند الرفض).
- ضروري قانونياً وتشغيلياً.

---

## 4. الموافقات — أين توضع؟

| الكيان | المسار في المنصة | المحتوى |
|--------|-------------------|---------|
| **موافقة المطاعم** | Admin → **Vendors** → **Applications** | بيانات، مستندات، فيديوهات، Signature items → Approve / Reject / Needs changes / Suspend |
| **موافقة السائقين** | Admin → **Drivers** → **Applications** | هوية، رخصة، سيارة، خلفية → Approve / Reject / Suspend / Probation |

كل الموافقات داخل نفس المنصة، واجهات مختلفة حسب الوحدة والدور.

---

## 5. Backend المطلوب (ملخص)

- **Auth للـ Admin:** نوع مستخدم `admin` أو جدول `admin_users` مع أدوار (يفضل جدول `roles` و `permissions`).
- **Guards:** التحقق من الدور والصلاحية قبل كل endpoint خاص بالإدارة.
- **Audit:** جدول `audit_logs` (user_id, action, entity, entity_id, old_value, new_value, reason, created_at).
- **Vendors:** endpoints جاهزة جزئياً؛ تحتاج: قائمة طلبات تسجيل، approve/reject/needs_changes مع سبب وكتابة في audit.
- **Drivers:** لديك بالفعل admin approve/reject؛ إضافة suspend/probation وربطها بـ audit.
- **Orders:** قائمة للادمن، تفاصيل، إلغاء/إعادة تعيين (حسب الصلاحية).
- **Monitoring:** قراءة فقط من الطلبات والسائقين والمطاعم (إحصائيات، نسب).
- **Risk:** قواعد بسيطة (مثلاً في الخدمة) تضع علامات أو تغيّر حالة (flag/suspend/review).

---

## 6. التقنيات المقترحة للـ Admin Panel

- **Frontend:** Next.js (مثل vendor-web) أو React + React Router.
- **UI:** Tailwind + مكونات (مثل shadcn/ui أو Ant Design) لواجهة جداول وفلترة وزراعات.
- **State:** Zustand أو React Query للبيانات والـ cache.
- **Auth:** JWT مع role في الـ payload؛ إعادة توجيه حسب الدور بعد Login.

---

## 7. الخطوات التالية المقترحة

1. **هيكل Admin Panel كامل (Pages + Modules + Roles)** — ✅ هذا المستند.
2. **Database schema للموافقات + audit_logs** — جداول وتدفق بيانات.
3. **Risk & Monitoring Rules** — قواعد جاهزة قابلة للتوسع.
4. **Roadmap تشغيل المنصة** — من يوم 1 حتى 100 ألف طلب.

يمكن البدء بإنشاء مشروع `admin_panel/` بهيكل المجلدات والصفحات أعلاه، ثم ربطه بالـ Backend خطوة بخطوة (Auth أولاً، ثم Vendors/Drivers approvals، ثم الباقي).
