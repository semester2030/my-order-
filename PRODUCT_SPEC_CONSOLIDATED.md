# المواصفة الموحّدة للمنتج — مرجع التنفيذ

> وثيقة واحدة تجمع أفضل القرارات من كل التحليلات السابقة. مرجع للفريق والمطورين.

---

## ١. الهوية والقرار الاستراتيجي

| الجملة | المعنى |
|--------|--------|
| **ما نبنيه** | Marketplace للخدمات الغذائية (مناسبات + طبخ منزلي). |
| **ما لسنا عليه** | تطبيق توصيل مطاعم تقليدي. |
| **الإطلاق** | مناسبات + طبخ منزلي فقط؛ المطاعم معطّلة (Feature Flag). |

**الهوية للمستخدم:** "نعرض طباخين بمهاراتهم؛ الفيديو هو الواجهة، والطلب يتم مباشرة من الشخص."

---

## ٢. نطاق الإطلاق (ما يظهر أولاً)

| الخدمة | الحالة عند الإطلاق |
|--------|---------------------|
| **المناسبات** (شعبية + خاصة) | ✅ أساسية |
| **الطبخ المنزلي** (وجبات جاهزة + خدمات عند الطلب) | ✅ أساسية |
| **المطاعم** (توصيل / قوائم) | ⏸️ مغلقة (Feature Flag) |

---

## ٣. الطبخ المنزلي — مساران بدون خلط

كل طباخة يمكن أن تقدم **نوعين** من الخدمة؛ التمييز صارم في الواجهة والـ Backend:

| المسار | الوصف | التدفق | الكارت |
|--------|--------|--------|--------|
| **🍱 وجبات جاهزة** | وجبات تتقنها الطباخة، معروضة في الفيديو وصفحتها | قائمة → أضف للسلة → Checkout | ✅ يُستخدم |
| **👩‍🍳 خدمات عند الطلب** | طلب مسبق (تاريخ، موقع، نوع طبخ) | نموذج طلب → إرسال → مناسباتي → قبول/عرض | ❌ لا يُستخدم |

**قاعدة:** لا خلط أسعار، لا توقعات خاطئة؛ الكارت للوجبات الجاهزة فقط.

---

## ٤. تجربة المستخدم (UX) — الطبخ المنزلي

### ٤.١ الفيديو (Feed)

- فيديو لكل طباخة (محتوى رئيسي).
- **أزرار على يمين الفيديو** (نمط تيك توك):
  - **🍱 وجبات جاهزة** → يفتح صفحة الطباخة (قائمة الوجبات) أو ي scroll لها.
  - **👩‍🍳 خدمات عند الطلب** → يفتح نموذج "اطلب طباخة" (إن كانت الخدمة مفعلة).

لا تبويبات منفصلة؛ الخياران على نفس الفيديو لكل طباخة.

**عرض الأزرار:** إن الطباخة تقدم نوعاً واحداً فقط → يظهر الزر المناسب فقط؛ أو الزر الثاني ظاهر ومعطّل مع توضيح "غير متوفر حالياً".

### ٤.٢ التسمية (الهوية ليست مطاعم)

| تجنّب | استخدم |
|--------|--------|
| View Restaurant | View Chef / عرض الطباخ |
| Restaurant Page | Chef Profile / صفحة الطباخ |
| Restaurant | Home Chef / مطبخ منزلي |
| Menu | الوجبات المتاحة / Dishes / Specialties |

تغيير **نصي وهويوي** فقط؛ لا تغيير بنيوي في الشاشات أو الـ Cart.

### ٤.٣ صفحة الطباخ (Chef Profile)

- أعلى الصفحة: الاسم، التقييم، الموقع (تقريبي)، نوع الخدمة: **🏠 طباخ منزلي**.
- **قائمة الوجبات** التي تتقنها: اسم، سعر، متاح/غير متاح، تصنيف (Signature / Regular).
- كل وجبة: **Add to Cart** كما هو الآن.
- (اختياري) وقت التحضير، حد أقصى للطلبات، ملاحظات.

### ٤.٤ السلة والدفع (وجبات جاهزة فقط)

- **نفس الـ Cart الحالي:** Subtotal، Delivery Fee (أو Pickup فقط)، Total، Checkout.
- الفرق المعنوي فقط: المصدر = طباخ منزلي، نوع الطلب = Ready Meal.
- **مرحلة أولى:** Pickup Only (لا توصيل، لا سائق، لا تتبع). لاحقاً: توصيل + وقت تسليم.

### ٤.٥ خدمات عند الطلب (اطلبي طباخة)

- نموذج: تاريخ، وقت، عدد أشخاص، مكان (في بيتك / تجهيز مسبق)، نوع الطبخ، ملاحظات.
- إرسال طلب → يظهر في **مناسباتي** → مقدم الخدمة يرد بقبول أو عرض سعر → العميل يقبل أو يرفض.
- لا سعر ولا دفع حتى القبول؛ لا استخدام للكارت في هذا المسار.

---

## ٥. المناسبات (خارج نطاق الطبخ المنزلي اليومي)

- **مناسبات شعبية** (ذبايح / طبخ منزلي للمناسبات) و **مناسبات خاصة** (بوفيه / ولائم).
- التدفق: **Request → Review → Accept → Execute** (لا Cart؛ event_requests + event_quotes).
- التفاصيل حسب وثيقة المناسبات (تاريخ، مكان، عدد أشخاص، عرض سعر، قبول، ثم دفع).

---

## ٦. الـ Backend — تمثيل موحّد بدون كسر

### ٦.١ المفهوم الموحّد: Provider

| بدل | استخدم |
|-----|--------|
| مطعم / طباخ / مزود مناسبات منفصل | **Service Provider** (كيان واحد بأصناف) |

**جدول مقترح: `providers`**

- `id`, `name`, `type` (restaurant \| home_chef \| event_provider)، `rating`, `location`, `is_active`
- `accepts_ready_meals` (وجبات جاهزة)
- `accepts_custom_requests` (خدمات عند الطلب / اطلبي طباخة)
- للمناسبات: نفس الـ provider أو نوع event_provider حسب نموذجك.

### ٦.٢ الوجبات الجاهزة

- **جدول:** `products` أو `ready_meals` مرتبط بـ `provider_id`.
- حقول: `name`, `price`, `is_available`, `prep_time`, `max_quantity` (اختياري)، `daily_limit` (اختياري)، `category` (signature / regular).
- **الطباخ المنزلي** = `provider.type = home_chef` مع منتجات في هذا الجدول.

### ٦.٣ الطلبات (Orders)

- **جدول:** `orders` (للوجبات الجاهزة والطلبات الفورية).
- حقول: `provider_id`, `customer_id`, `order_type` (ready_meal \| custom \| event)، `scheduled_time`, `status`.
- **لا تربط** طلبات "خدمات عند الطلب" بنفس مسار السلة؛ الطلبات المسبقة/المناسبات في `event_requests` و `event_quotes`.

### ٦.٤ الطلبات المسبقة والمناسبات

- **جداول:** `event_requests`, `event_quotes` (منفصلة عن `orders`).
- للاطلبي طباخة والمناسبات: Request → Quote/Accept → ثم عند القبول يمكن ربط دفع أو order تأكيدي حسب تصميمك.

---

## ٧. قواعد تشغيلية (لا تتنازل عنها في المرحلة الأولى)

| القاعدة | التطبيق |
|---------|----------|
| **وجبات جاهزة** | Pickup Only — لا توصيل، لا سائق، لا تتبع. |
| **خدمات عند الطلب / المناسبات** | Request فقط — لا سعر ولا دفع حتى القبول أو قبول العرض. |
| **الكارت** | للوجبات الجاهزة فقط؛ لا استخدام له في اطلبي طباخة أو المناسبات. |
| **الهوية** | كل النصوص والتصنيفات = "طباخ منزلي / خدمات منزلية"، وليس "مطعم". |

لاحقاً (Phase 2): توصيل للوجبات الجاهزة، تتبع، جدولة، إلخ.

---

## ٨. القائمة السفلية (Bottom Nav) في وضع الإطلاق

- **مناسباتي:** طلبات المناسبات + طلبات "خدمات عند الطلب" (اطلبي طباخة).
- **حسابي:** البروفايل والإعدادات.
- **Cart:** يظهر **فقط** عندما يوجد عناصر من "وجبات جاهزة".
- لا تبويب "Orders" منفصل للتمييز النوعي؛ الفصل (وجبات جاهزة vs مناسبات/خدمات عند الطلب) **داخل** صفحة الطلبات/مناسباتي إن لزم.

(يمكن توحيدها تحت "طلباتي" مع فلتر: وجبات جاهزة | مناسباتي.)

---

## ٩. لوحة مقدم الخدمة (Vendor / Provider Panel)

- **وجبات اليوم:** قائمة الوجبات، الكمية المتبقية، الطلبات الجديدة.
- إجراءات: إيقاف وجبة، تعديل سعر، تحديد وقت إغلاق (أو daily_limit).
- **طلبات "خدمات عند الطلب" والمناسبات:** قائمة طلبات، رد بعرض أو قبول/رفض.
- نفس منطق لوحة المطعم قدر الإمكان مع إخفاء ما لا يلزم (طاولات، بوفيه إن لم يكن ضمن النطاق).

---

## ١٠. ما لا نغيّره (تقنياً)

| المكوّن | القرار |
|---------|--------|
| **Cart** | كما هو؛ يُستخدم للوجبات الجاهزة فقط. |
| **Checkout** | كما هو؛ مع Pickup Only أولاً. |
| **Feed** | كما هو؛ نضيف الأزرار الجانبية ونغيّر التسميات. |
| **صفحة المطعم/الطباخ** | نفس الصفحة؛ نغيّر النصوص والهوية إلى "Chef / مطبخ منزلي". |
| **Orders (جداول/API)** | نوسّع بـ `order_type` و `provider_id`؛ لا هدم. |

---

## ١١. ملخص تنفيذي للفريق

1. **الهوية:** منصة خدمات أكل (مناسبات + طبخ منزلي)، وليس تطبيق مطاعم.
2. **الفيديو:** أزرار على اليمين (وجبات جاهزة + خدمات عند الطلب)؛ لا تبويبات للطبخ المنزلي.
3. **التسمية:** View Chef، صفحة الطباخ، الوجبات المتاحة؛ لا Restaurant / Menu.
4. **الكارت:** وجبات جاهزة فقط؛ اطلبي طباخة والمناسبات = Request/Accept بدون كارت.
5. **Backend:** providers (بأنواع)، products/ready_meals، orders (order_type)، event_requests/event_quotes منفصلة.
6. **الإطلاق:** مناسبات + طبخ منزلي؛ المطاعم مغلقة بـ Feature Flag.
7. **المرحلة الأولى:** Pickup Only للوجبات الجاهزة؛ لا توصيل ولا سائق.

هذه الوثيقة هي **المرجع الموحّد**؛ يمكن الرجوع إلى التحليلات السابقة للتفاصيل أو السيناريوهات، لكن القرارات النهائية للتنفيذ هي ما ورد أعلاه.
