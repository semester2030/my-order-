# تنسيق المسارين داخل "الطبخ المنزلي"

## المشكلة

داخل **الطبخ المنزلي** يوجد نوعان من الطلب:

| النوع | الوصف | التدفق |
|-------|--------|--------|
| **اطبي طباخة** | طلب مسبق: نوع الطبخ، الموقع، التاريخ → مقدم الخدمة يقبل أو يرسل عرض | Request → Accept (لا سلة، لا سعر فوري) |
| **وجبات جاهزة** | منتجات جاهزة، كمية محدودة، اطلب الآن | Cart → Checkout (سلة، سعر ثابت، Pickup) |

المطلوب: تنسيق واضح بين الاثنين دون أن يشعر المستخدم أنه دخل "مطاعم"، مع استغلال الكارت الحالي للوجبات الجاهزة فقط.

---

## ١. الهيكل من منظور المستخدم

### خيار أ: تبويبان (Tab 1 / Tab 2)

بعد الضغط على أيقونة **الطبخ المنزلي** يصل المستخدم إلى شاشة فيها **تبويبان**:

```
┌─────────────────────────────────────────┐
│  الطبخ المنزلي                          │
├─────────────────────────────────────────┤
│  [Tab 1: 👩‍🍳 اطلبي طباخة]  [Tab 2: 🍱 وجبات جاهزة]  │
├─────────────────────────────────────────┤
│  محتوى التبويب المختار                   │
└─────────────────────────────────────────┘
```

- **Tab 1 – اطلبي طباخة:** قائمة مقدمي الخدمة مع فيديو، صور، تقييم. لكل واحد زر **"اطلب طباخة"** → شاشة طلب مناسبة → إرسال → مناسباتي.
- **Tab 2 – وجبات جاهزة:** قائمة مقدمي الخدمة / وجبات جاهزة. زر **"أضف للسلة"** → Cart → Checkout (Pickup Only).

---

### خيار ب (مُفضّل): أزرار على يمين الفيديو — نفس تيك توك

بدلاً من تبويبات منفصلة، **كل فيديو (كل طباخة) يكون عليه على الجانب الأيمن زرّان**:

- **وجبات جاهزة** — يفتح قائمة وجباتها الجاهزة → أضف للسلة → Checkout.
- **خدمات عند الطلب** — يفتح نموذج "اطلب طباخة" (تاريخ، موقع، نوع طبخ…) → إرسال طلب → مناسباتي.

```
┌──────────────────────────────────┬─────┐
│                                   │ 🍱  │  ← وجبات جاهزة
│         فيديو الطباخة             │     │
│                                   │ 👩‍🍳 │  ← خدمات عند الطلب
│                                   ├─────┤
│                                   │ ♥   │
│                                   │ …   │
└──────────────────────────────────┴─────┘
```

**لماذا هذا مناسب؟**

| الجانب | الفائدة |
|--------|---------|
| **واحد فيدوهات** | المستخدم ما يضطر يغير تبويب؛ يشوف الطباخة ويختار مباشرة "جاهزة" أو "عند الطلب". |
| **كل طباخة يتاح لها النوعين** | نفس الشاشة تعرض الخيارين لكل مقدم خدمة — إن عندها جاهزة و/أو عند الطلب. |
| **نمط مألوف** | مثل تيك توك / Reels: أزرار على اليمين؛ سهل الفهم والاستخدام. |
| **وضوح الهوية** | الأيقونات على الفيديو توضح: "هذه خدمات منزلية — إما جاهزة وإما حجز" وليست مطاعم. |

**قواعد عرض الأزرار:**

- إن الطباخة **عندها وجبات جاهزة فقط** → يظهر زر "وجبات جاهزة" فقط (أو الزر الثاني معطل مع توضيح "لا توجد خدمات عند الطلب حالياً").
- إنها **تقبل خدمات عند الطلب فقط** → يظهر زر "خدمات عند الطلب" فقط.
- إنها تقدم **الاثنين** → يظهر الزرّان (كما في المثال أعلاه).

يعني: **نفس الفيديو، نفس الشاشة، كل طباخة يتاح لها الخياران حسب ما تفعّله في لوحتها** — بدون تبويبات، وبدون شعور أن المستخدم دخل "قائمة مطاعم".

---

### مقارنة سريعة

| المعيار | تبويبان | أزرار على يمين الفيديو (تيك توك) |
|---------|---------|-----------------------------------|
| عدد النقرات | تبويب ثم اختيار | اختيار مباشر من الفيديو |
| وضوح الخيارين لكل طباخة | يلزم تغيير التبويب لرؤية النوع الثاني | الخياران ظاهران مع كل فيديو |
| نمط مألوف | قوائم/تصنيفات | فيدوهات + أزرار جانبية (تيك توك) |
| التوصية | ممكن للمحتوى كثير نص/قوائم | **أفضل للفيديو أولاً** — أن الفيديو هو المحتوى الرئيسي والزرّان جزء من تجربة المشاهدة. |

**الخلاصة:** لو الشاشة الرئيسية في "الطبخ المنزلي" هي **فيديو (فيديو لكل طباخة)**، فالأفضل أن تكون **أزرار "وجبات جاهزة" و "خدمات عند الطلب" على يمين الفيديو** — كل طباخة يتاح لها هذا الطلب من نفس الشاشة دون تبويبات.

---

## ٢. قواعد التشغيل (لا تتنازل عنها في المرحلة الأولى)

| الجانب | القاعدة |
|--------|---------|
| **وجبات جاهزة (Tab 2)** | Pickup Only — لا توصيل، لا سائق، لا تتبع. |
| **اطبي طباخة (Tab 1)** | Request فقط — لا سعر ولا دفع حتى يقبل مقدم الخدمة أو يرسل عرض والعميل يقبل. |
| **الهوية** | الأيقونة والتصنيف "الطبخ المنزلي" = خدمات منزلية، وليست مطاعم. |

لاحقاً (Phase 2): إمكان إضافة توصيل للوجبات الجاهزة + تتبع، مع بقاء "اطبي طباخة" على نموذج الطلب/العرض/القبول.

---

## ٣. التنسيق بين المسارين (Backend ببساطة)

### كيانات منفصلة = عدم لخبطة

| المسار | الكيان الرئيسي | متى يُستخدم |
|--------|-----------------|-------------|
| **وجبات جاهزة (Tab 2)** | `ready_meals` (أو `menu_items` مع `category = home_cooking` و `pickup_only = true`) | منتجات جاهزة، سعر ثابت، سلة، طلب فوري. |
| **اطبي طباخة (Tab 1)** | `event_requests` (و `event_quotes` عند وجود عرض سعر) | طلب مسبق (تاريخ، موقع، نوع طبخ)، لا سعر ولا دفع حتى القبول. |

- **لا تربط** `ready_meals` بـ `event_requests` أو بالحجوزات (reservations). كل مسار له جدولته وتدفقه.
- **الربط الوحيد:** نفس `provider_id` (مقدم الخدمة) يمكن أن يكون له:
  - وجبات جاهزة (في Tab 2)، و/أو
  - قبول طلبات "اطبي طباخة" (في Tab 1).

يعني: مقدم الخدمة قد يظهر في التبويبين؛ في Tab 1 زر "اطلب طباخة"، وفي Tab 2 قائمة وجباته الجاهزة مع "أضف للسلة".

---

## ٤. كيف يظهر مقدم الخدمة في التبويبين؟

| الحالة | Tab 1 (اطبي طباخة) | Tab 2 (وجبات جاهزة) |
|--------|---------------------|----------------------|
| عنده وجبات جاهزة فقط | لا يظهر (أو يظهر مع رسالة "لا يقبل طلبات طباخة حالياً") | يظهر مع وجباته + أضف للسلة |
| يقبل طلبات طباخة فقط | يظهر مع "اطلب طباخة" | لا يظهر (أو يظهر مع "لا توجد وجبات جاهزة حالياً") |
| الاثنين معاً | يظهر مع "اطلب طباخة" | يظهر مع وجباته + أضف للسلة |

يفضل في الـ Backend:
- حقل أو علاقة مثل `accepts_home_cooking_requests` (يقبل طلبات "اطبي طباخة").
- وحقل أو علاقة مثل `has_ready_meals` (عنده وجبات جاهزة للبيع).

وواجهة مقدم الخدمة (Vendor Web) تسمح له بتفعيل: "أقبل طلبات طباخة"، "أعرض وجبات جاهزة"، أو الاثنين.

---

## ٥. الـ UX في التطبيق (خلاصة)

**التوصية:** لو الشاشة الرئيسية في "الطبخ المنزلي" هي **فيديو** (فيديو لكل طباخة)، استخدم **أزرار على يمين الفيديو** (نفس تيك توك):

- زر **وجبات جاهزة** → قائمة الوجبات → سلة → Checkout (Pickup Only).
- زر **خدمات عند الطلب** → نموذج طلب طباخة → إرسال → مناسباتي.

كل طباخة يتاح لها الخياران من نفس الفيديو؛ لا حاجة لتبويبات. إن الطباخة تقدم نوعاً واحداً فقط، يظهر الزر المناسب فقط.

**بديل:** لو المحتوى قوائم/بطاقات أكثر من فيديو، استخدم تبويبان: **اطبي طباخة** | **وجبات جاهزة**.

**قواعد ثابتة (أي خيار):**

1. وجبات جاهزة = سلة + Checkout (Pickup Only)؛ لا توصيل في المرحلة الأولى.
2. خدمات عند الطلب = Request → Accept؛ لا سعر ولا دفع حتى القبول.
3. Bottom Nav (وضع مناسبات فقط): مناسباتي، حسابي؛ الكارت يظهر فقط عند وجود منتجات من "وجبات جاهزة".

بهذا ننسق بين الطلبات المسبقة والوجبات الجاهزة دون لخبطة، مع الاستفادة من الكارت الحالي بدون إعادة بناء.
