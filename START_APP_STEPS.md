# خطوات تشغيل التطبيق

## ⚠️ PostgreSQL توقف - يجب إعادة تشغيله أولاً

---

## 🚀 الخطوات بالترتيب:

### الخطوة 1: إعادة تشغيل PostgreSQL

```bash
brew services start postgresql@14
```

### الخطوة 2: التحقق

```bash
pg_isready
```

**النتيجة المتوقعة:**
```
/tmp:5432 - accepting connections
```

---

### الخطوة 3: تشغيل التطبيق

```bash
cd "/Users/fayez/Desktop/my order/backend"
npm run start:dev
```

**ما سيحدث:**
- TypeORM سيتصل بقاعدة البيانات
- سينشئ جميع الجداول تلقائياً
- التطبيق سيعمل على `http://localhost:3000`

---

### الخطوة 4: التحقق من الجداول (في terminal آخر)

```bash
psql -d customer_app -c "\dt"
```

**سترى جميع الجداول المنشأة**

---

## 📋 جميع الأوامر (نسخ ولصق):

```bash
# 1. إعادة تشغيل PostgreSQL
brew services start postgresql@14

# 2. التحقق
pg_isready

# 3. تشغيل التطبيق
cd "/Users/fayez/Desktop/my order/backend"
npm run start:dev
```

---

## ⚠️ ملاحظة مهمة:

**PostgreSQL قد يتوقف تلقائياً.** 

**الحل الدائم:**
- أضفه كـ service تلقائي: `brew services start postgresql@14`
- أو شغّله يدوياً كل مرة قبل تشغيل التطبيق

---

## ✅ بعد تشغيل التطبيق:

سترى في Terminal:
```
Application is running on: http://localhost:3000
```

**هذا يعني:**
- ✅ PostgreSQL متصل
- ✅ الجداول تم إنشاؤها
- ✅ التطبيق يعمل

---

## 🎯 الخلاصة:

1. **شغّل PostgreSQL:** `brew services start postgresql@14`
2. **شغّل التطبيق:** `npm run start:dev`
3. **TypeORM سينشئ الجداول تلقائياً**

**لا نحتاج Migrations!**
