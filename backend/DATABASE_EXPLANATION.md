# 📊 شرح قاعدة البيانات

## ✅ الوضع الحالي (صحيح)

### Backend واحد لجميع التطبيقات
- **Backend:** NestJS واحد يخدم:
  - Customer App (تطبيق العميل)
  - Driver App (تطبيق السائق)
  - Vendor App (تطبيق المطعم) - إذا كان موجود

### قاعدة بيانات واحدة
- **Database Name:** `customer_app`
- **السبب:** كل التطبيقات تستخدم نفس الـ Backend ونفس قاعدة البيانات
- **الجداول المشتركة:**
  - `users` - المستخدمون (عملاء + سائقون + مطاعم)
  - `drivers` - السائقون
  - `vendors` - المطاعم
  - `orders` - الطلبات
  - `jobs` - عروض العمل للسائقين
  - وغيرها...

## ✅ هذا التصميم صحيح لأن:

1. **مشاركة البيانات:** الطلبات تحتاج معلومات من العملاء والسائقين والمطاعم
2. **Backend موحد:** API واحد يخدم جميع التطبيقات
3. **إدارة أسهل:** قاعدة بيانات واحدة أسهل في الإدارة
4. **علاقات واضحة:** العلاقات بين الجداول (Orders → Users → Drivers) تعمل بشكل صحيح

## 📋 الجداول في قاعدة البيانات `customer_app`:

### جداول المستخدمين:
- `users` - جميع المستخدمين (عملاء، سائقون، مطاعم)
- `drivers` - السائقون (مرتبط بـ users)
- `vendors` - المطاعم (مرتبط بـ users)

### جداول الطلبات:
- `orders` - الطلبات
- `order_items` - عناصر الطلب
- `jobs` - عروض العمل للسائقين

### جداول أخرى:
- `addresses` - العناوين
- `cart` - سلة التسوق
- `payments` - المدفوعات
- وغيرها...

## ✅ إذن:

**لا يوجد خلل!** ✅

- ✅ Backend واحد يخدم جميع التطبيقات
- ✅ قاعدة بيانات واحدة (`customer_app`) تحتوي جميع الجداول
- ✅ SQL scripts للسائقين يجب أن تشغّل على `customer_app`
- ✅ هذا التصميم صحيح ومنطقي

## 🔧 كيفية استخدام SQL Scripts:

```bash
# الطريقة الصحيحة:
psql -d customer_app -f backend/RESET_TEST_DRIVER_ACCOUNT.sql
psql -d customer_app -f backend/VERIFY_TEST_DRIVER.sql
```

**ملاحظة:** `customer_app` هو اسم قاعدة البيانات الصحيح! ✅
